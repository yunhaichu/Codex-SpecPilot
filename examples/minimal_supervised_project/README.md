# Minimal Supervised Project

This is a tiny example project for testing Codex SpecPilot supervised workflow.

The project contains a minimal calculator module with `add(a, b)` and
`subtract(a, b)` functions.

Run the standard-library tests with:

```bash
python run_tests.py
```
